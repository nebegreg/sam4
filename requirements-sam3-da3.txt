# Requirements for SAM3 Roto Ultimate
# Compatible with SAM3 + Depth Anything 3
# Date: 2025-12-03
# Python: 3.12
# CUDA: 12.8 or 12.6

# PyTorch and Vision (CUDA 12.8)
# Install with: pip install -r requirements-sam3-da3.txt -f https://download.pytorch.org/whl/cu128
torch==2.7.1
torchvision==0.22.1
torchaudio==2.7.1

# Transformers (main branch required for SAM3)
# Install with: pip install git+https://github.com/huggingface/transformers.git
# transformers>=4.57.0  # Uncomment when SAM3 is in stable release

# Core ML Libraries
numpy>=1.26.0
pillow>=10.0.0
opencv-python>=4.8.0
scipy>=1.11.0
matplotlib>=3.7.0
scikit-image>=0.21.0
scikit-learn>=1.3.0

# Deep Learning Utilities
einops>=0.7.0
timm>=0.9.0
xformers>=0.0.23

# Hugging Face
huggingface-hub>=0.20.0
accelerate>=0.25.0
sentencepiece>=0.1.99
protobuf>=4.25.0

# GUI Framework
PySide6>=6.5.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# SAM3 (install separately from GitHub)
# cd /tmp && git clone https://github.com/facebookresearch/sam3.git && cd sam3 && pip install -e .

# Depth Anything 3 (install separately from GitHub)
# cd /tmp && git clone https://github.com/ByteDance-Seed/Depth-Anything-3.git && cd Depth-Anything-3 && pip install -e ".[all]"
